<%- include('include/header', {title: 'Déclarer un bug ou une amélioration'}); %>
    <% if (typeof declared != 'undefined' && declared === true) { %>
        <p class="declared">Merci pour votre contribution !</p>
        <% } %>
    <% if (typeof error != 'undefined') { %>
        <p class="declared-error">Erreur : <%- error %> !</p>
    <% } %>    
	<p>Merci de nous aider à améliorer notre application en signalant un bug ou en proposant une amélioration.</p>
	<form action="issue" method="post">
		<label for="type">Type :</label>
		<select name="type" id="type">
			<option value="bug">Bug</option>
			<option value="amélioration">Amélioration</option>
		</select>
		<br>
		<label for="description">Description :</label>
		<textarea name="description" id="description" rows="5" required></textarea>
		<br>
		<label for="name">Nom :</label>
		<input type="name" name="name" id="name" value="<%- playerName %>" required>
		<br>
		<label for="email">Adresse email :</label>
		<input type="email" name="email" id="email" <% if (typeof email != 'undefined' ) { %>value="<%- email %>"<% } %> required>
		<br>
		<button type="submit">Envoyer</button>
	</form>
<%- include('include/footer'); %>